[package]
name = "integration"
version = "0.1.0"
edition = "2021"

[dependencies]
core_world = { path = "../core_world" }
physics_world = { path = "../physics_world" }
serde_json = "1.0"

[dev-dependencies]
proptest = "1.0"
criterion = "0.4"