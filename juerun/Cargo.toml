[package]
name = "juerun"
version = "0.1.0"
edition = "2021"

[dependencies]
jue_common = { path = "../jue_common" }
juec = { path = "../juec" }

# JIT backend support
cranelift = "0.126.1"
cranelift-module = "0.126.1"
cranelift-jit = "0.126.1"
anyhow = "1.0"

[features]
default = []
jit = []
